class EnemyManager {

}

class Enemy extends Entity {
  update(dt) {
    /* Update method(moves the character, etc...)
      * params:
        - dt: Number "The game's delta time"
      * returns:
          null
    */

    if ()
  }
}
